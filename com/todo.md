# Offene Punkte

Kurz halten. Vor jeder Aufgabe lesen. Erledigtes loeschen oder nach `doc/` verschieben.

## piCon (Container)

- `Nodges_Pi/docker-compose.yml` unter Windows liegt noch ausserhalb des Repos (`C:\Users\ich\Desktop\code\_projects\Nodges_Pi\docker-compose.yml`); der Ordner `Nodges_Pi/` im Repo ist nur ein Git-Snapshot. Bei Gelegenheit harmonisieren (siehe `resetup.md`).
- Lokaler Branch `pi` ist remote geloescht; zur Aufraeumung vorgemerkt (siehe `bericht.md` Teil F.5).
- moegliche Ergaenzung: Backup-Skript fuer `lightrag-backend/rag_storage/`, da Vektordaten nicht in Git liegen.

## conT (Antigravity `W:\`)

- Alte Dokumente unter `com/doc/` nennen teilweise noch `origin/pi` statt `origin/main` (z.B. `0_106_0_entscheidung_dual_harness...`). Die Branch-Angabe ist ueberholt, Inhalte teils weiter relevant.

## Gemeinsam / unklar

- Soll ein **`Co-authored-by`-Trailer** in Commits eingefuehrt werden, damit Banixx Repo-Owner bleibt und KI-Beitraege transparent sind? (Empfehlung von winAnt, noch nicht umgesetzt — `setup.md` Abschnitt 5.)
- Kein GitHub-MCP beschlossen; fuer Issues/PRs stattdessen die `gh` CLI nutzen.
