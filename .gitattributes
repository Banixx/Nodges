# Auto-detect text files and normalize line endings to LF on commit
* text=auto eol=lf

# Explicitly ensure shell scripts use LF
*.sh text eol=lf

# Windows-specific scripts
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
