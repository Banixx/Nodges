# Task Liste: Cloud-Schwerpunkt Visualisierung

- `[x]` **Phase 1: NodeManager Rendering (Visualisierung)**
  - `[x]` Definition des `cloud` Materials (transluzent, abweichende Farbe z.B. 0x64ffda, opacity 0.2) in `initializeMaterials`.
  - `[x]` Zuordnung von `SphereGeometry` für den Typ `cloud` in `initializeGeometries`.
  - `[x]` Implementierung von `updateCloudPositions(relationships, allEntities)`, welche den (x,y,z) Schwerpunkt und den umhüllenden Radius für jede Cloud basierend auf ihren verknüpften Knoten dynamisch berechnet.

- `[x]` **Phase 2: App-Loop Integration**
  - `[x]` Aufruf von `this.nodeManager.updateCloudPositions()` in der Haupt-Render-Schleife (`App.ts`), damit sich die Hülle an jede Positionsänderung (egal ob durch Physik oder explizite Mappings) in Echtzeit anpasst.
