flowchart LR
    A[Benutzer] -->|Interaktion| B[CentralEventManager]
    B -->|Ereignis| C[InteractionManager]
    C -->|Zustandsänderung| D[StateManager]
    D -->|Benachrichtigung| E[HighlightManager]
    D -->|Benachrichtigung| F[UIManager]
    G[Dateisystem] -->|JSON-Daten| H[FileHandler]
    H -->|Netzwerkdaten| I[StateManager]
    I -->|Nodes/Edges| J[LayoutManager]
    J -->|Positionen| K[Rendering]
    K -->|3D-Szene| L[Three.js]
